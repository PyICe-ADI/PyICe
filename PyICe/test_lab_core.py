"""Tests for lab core.

>>> from PyICe.test_lab_core import TestChannelGroup

"""
class TestChannelGroup:
    """Tests for Channel Group.

    >>> from PyICe.test_lab_core import TestChannelGroup
    >>> TestChannelGroup is not None
    True

    """
    def test_copy(self):
        """Perform test copy operation.

        Exercises the unit under test and asserts expected behavior.

        >>> from PyICe.test_lab_core import TestChannelGroup
        >>> hasattr(TestChannelGroup, 'test_copy')
        True

        """
        assert False

    def test_get_name(self):
        """Perform test get name operation.

        Exercises the unit under test and asserts expected behavior.

        >>> from PyICe.test_lab_core import TestChannelGroup
        >>> hasattr(TestChannelGroup, 'test_get_name')
        True

        """
        assert False

    def test_set_name(self):
        """Perform test set name operation.

        Exercises the unit under test and asserts expected behavior.

        >>> from PyICe.test_lab_core import TestChannelGroup
        >>> hasattr(TestChannelGroup, 'test_set_name')
        True

        """
        assert False

    def test_get_categories(self):
        """Perform test get categories operation.

        Exercises the unit under test and asserts expected behavior.

        >>> from PyICe.test_lab_core import TestChannelGroup
        >>> hasattr(TestChannelGroup, 'test_get_categories')
        True

        """
        assert False

    def test_sort(self):
        """Perform test sort operation.

        Exercises the unit under test and asserts expected behavior.

        >>> from PyICe.test_lab_core import TestChannelGroup
        >>> hasattr(TestChannelGroup, 'test_sort')
        True

        """
        assert False

    def test_add(self):
        """Perform test add operation.

        Exercises the unit under test and asserts expected behavior.

        >>> from PyICe.test_lab_core import TestChannelGroup
        >>> hasattr(TestChannelGroup, 'test_add')
        True

        """
        assert False

    def test__add_channel(self):
        """Perform test  add channel operation.

        Exercises the unit under test and asserts expected behavior.

        >>> from PyICe.test_lab_core import TestChannelGroup
        >>> hasattr(TestChannelGroup, 'test__add_channel')
        True

        """
        assert False

    def test_merge_in_channel_group(self):
        """Perform test merge in channel group operation.

        Exercises the unit under test and asserts expected behavior.

        >>> from PyICe.test_lab_core import TestChannelGroup
        >>> hasattr(TestChannelGroup, 'test_merge_in_channel_group')
        True

        """
        assert False

    def test__add_sub_channel_group(self):
        """Perform test  add sub channel group operation.

        Exercises the unit under test and asserts expected behavior.

        >>> from PyICe.test_lab_core import TestChannelGroup
        >>> hasattr(TestChannelGroup, 'test__add_sub_channel_group')
        True

        """
        assert False

    def test_get_channel_groups(self):
        """Perform test get channel groups operation.

        Exercises the unit under test and asserts expected behavior.

        >>> from PyICe.test_lab_core import TestChannelGroup
        >>> hasattr(TestChannelGroup, 'test_get_channel_groups')
        True

        """
        assert False

    def test_read(self):
        """Perform test read operation.

        Exercises the unit under test and asserts expected behavior.

        >>> from PyICe.test_lab_core import TestChannelGroup
        >>> hasattr(TestChannelGroup, 'test_read')
        True

        """
        assert False

    def test_write(self):
        """Perform test write operation.

        Exercises the unit under test and asserts expected behavior.

        >>> from PyICe.test_lab_core import TestChannelGroup
        >>> hasattr(TestChannelGroup, 'test_write')
        True

        """
        assert False

    def test_read_channel(self):
        """Perform test read channel operation.

        Exercises the unit under test and asserts expected behavior.

        >>> from PyICe.test_lab_core import TestChannelGroup
        >>> hasattr(TestChannelGroup, 'test_read_channel')
        True

        """
        assert False

    def test_read_channels(self):
        """Perform test read channels operation.

        Exercises the unit under test and asserts expected behavior.

        >>> from PyICe.test_lab_core import TestChannelGroup
        >>> hasattr(TestChannelGroup, 'test_read_channels')
        True

        """
        assert False

    def test_write_channel(self):
        """Perform test write channel operation.

        Exercises the unit under test and asserts expected behavior.

        >>> from PyICe.test_lab_core import TestChannelGroup
        >>> hasattr(TestChannelGroup, 'test_write_channel')
        True

        """
        assert False

    def test_write_channels(self):
        """Perform test write channels operation.

        Exercises the unit under test and asserts expected behavior.

        >>> from PyICe.test_lab_core import TestChannelGroup
        >>> hasattr(TestChannelGroup, 'test_write_channels')
        True

        """
        assert False

    def test_get_channel(self):
        """Perform test get channel operation.

        Exercises the unit under test and asserts expected behavior.

        >>> from PyICe.test_lab_core import TestChannelGroup
        >>> hasattr(TestChannelGroup, 'test_get_channel')
        True

        """
        assert False

    def test_get_flat_channel_group(self):
        """Perform test get flat channel group operation.

        Exercises the unit under test and asserts expected behavior.

        >>> from PyICe.test_lab_core import TestChannelGroup
        >>> hasattr(TestChannelGroup, 'test_get_flat_channel_group')
        True

        """
        assert False

    def test__resolve_channel(self):
        """Perform test  resolve channel operation.

        Exercises the unit under test and asserts expected behavior.

        >>> from PyICe.test_lab_core import TestChannelGroup
        >>> hasattr(TestChannelGroup, 'test__resolve_channel')
        True

        """
        assert False

    def test_get_all_channels_dict(self):
        """Perform test get all channels dict operation.

        Exercises the unit under test and asserts expected behavior.

        >>> from PyICe.test_lab_core import TestChannelGroup
        >>> hasattr(TestChannelGroup, 'test_get_all_channels_dict')
        True

        """
        assert False

    def test_get_all_channel_names(self):
        """Perform test get all channel names operation.

        Exercises the unit under test and asserts expected behavior.

        >>> from PyICe.test_lab_core import TestChannelGroup
        >>> hasattr(TestChannelGroup, 'test_get_all_channel_names')
        True

        """
        assert False

    def test_get_all_channels_list(self):
        """Perform test get all channels list operation.

        Exercises the unit under test and asserts expected behavior.

        >>> from PyICe.test_lab_core import TestChannelGroup
        >>> hasattr(TestChannelGroup, 'test_get_all_channels_list')
        True

        """
        assert False

    def test_get_all_channels_set(self):
        """Perform test get all channels set operation.

        Exercises the unit under test and asserts expected behavior.

        >>> from PyICe.test_lab_core import TestChannelGroup
        >>> hasattr(TestChannelGroup, 'test_get_all_channels_set')
        True

        """
        assert False

    def test_read_channel_list(self):
        """Perform test read channel list operation.

        Exercises the unit under test and asserts expected behavior.

        >>> from PyICe.test_lab_core import TestChannelGroup
        >>> hasattr(TestChannelGroup, 'test_read_channel_list')
        True

        """
        assert False

    def test__read_channels_non_threaded(self):
        """Perform test  read channels non threaded operation.

        Exercises the unit under test and asserts expected behavior.

        >>> from PyICe.test_lab_core import TestChannelGroup
        >>> hasattr(TestChannelGroup, 'test__read_channels_non_threaded')
        True

        """
        assert False

    def test__read_channels_threaded(self):
        """Perform test  read channels threaded operation.

        Exercises the unit under test and asserts expected behavior.

        >>> from PyICe.test_lab_core import TestChannelGroup
        >>> hasattr(TestChannelGroup, 'test__read_channels_threaded')
        True

        """
        assert False

    def test_start_threads(self):
        """Perform test start threads operation.

        Exercises the unit under test and asserts expected behavior.

        >>> from PyICe.test_lab_core import TestChannelGroup
        >>> hasattr(TestChannelGroup, 'test_start_threads')
        True

        """
        assert False

    def test_threaded_read_function(self):
        """Perform test threaded read function operation.

        Exercises the unit under test and asserts expected behavior.

        >>> from PyICe.test_lab_core import TestChannelGroup
        >>> hasattr(TestChannelGroup, 'test_threaded_read_function')
        True

        """
        assert False

    def test_get_threaded_results(self):
        """Perform test get threaded results operation.

        Exercises the unit under test and asserts expected behavior.

        >>> from PyICe.test_lab_core import TestChannelGroup
        >>> hasattr(TestChannelGroup, 'test_get_threaded_results')
        True

        """
        assert False

    def test_read_all_channels(self):
        """Perform test read all channels operation.

        Exercises the unit under test and asserts expected behavior.

        >>> from PyICe.test_lab_core import TestChannelGroup
        >>> hasattr(TestChannelGroup, 'test_read_all_channels')
        True

        """
        assert False

    def test_remove_channel(self):
        """Perform test remove channel operation.

        Exercises the unit under test and asserts expected behavior.

        >>> from PyICe.test_lab_core import TestChannelGroup
        >>> hasattr(TestChannelGroup, 'test_remove_channel')
        True

        """
        assert False

    def test_remove_channel_group(self):
        """Perform test remove channel group operation.

        Exercises the unit under test and asserts expected behavior.

        >>> from PyICe.test_lab_core import TestChannelGroup
        >>> hasattr(TestChannelGroup, 'test_remove_channel_group')
        True

        """
        assert False

    def test_remove_channel_by_name(self):
        """Perform test remove channel by name operation.

        Exercises the unit under test and asserts expected behavior.

        >>> from PyICe.test_lab_core import TestChannelGroup
        >>> hasattr(TestChannelGroup, 'test_remove_channel_by_name')
        True

        """
        assert False

    def test_remove_all_channels_and_sub_groups(self):
        """Perform test remove all channels and sub groups operation.

        Exercises the unit under test and asserts expected behavior.

        >>> from PyICe.test_lab_core import TestChannelGroup
        >>> hasattr(TestChannelGroup, 'test_remove_all_channels_and_sub_groups')
        True

        """
        assert False

    def test_remove_sub_channel_group(self):
        """Perform test remove sub channel group operation.

        Exercises the unit under test and asserts expected behavior.

        >>> from PyICe.test_lab_core import TestChannelGroup
        >>> hasattr(TestChannelGroup, 'test_remove_sub_channel_group')
        True

        """
        assert False

    def test_remove_category(self):
        """Perform test remove category operation.

        Exercises the unit under test and asserts expected behavior.

        >>> from PyICe.test_lab_core import TestChannelGroup
        >>> hasattr(TestChannelGroup, 'test_remove_category')
        True

        """
        assert False

    def test_remove_categories(self):
        """Perform test remove categories operation.

        Exercises the unit under test and asserts expected behavior.

        >>> from PyICe.test_lab_core import TestChannelGroup
        >>> hasattr(TestChannelGroup, 'test_remove_categories')
        True

        """
        assert False

    def test_debug_print(self):
        """Perform test debug print operation.

        Exercises the unit under test and asserts expected behavior.

        >>> from PyICe.test_lab_core import TestChannelGroup
        >>> hasattr(TestChannelGroup, 'test_debug_print')
        True

        """
        assert False

    def test_remove_channel_list(self):
        """Perform test remove channel list operation.

        Exercises the unit under test and asserts expected behavior.

        >>> from PyICe.test_lab_core import TestChannelGroup
        >>> hasattr(TestChannelGroup, 'test_remove_channel_list')
        True

        """
        assert False

    def test_resolve_channel_list(self):
        """Perform test resolve channel list operation.

        Exercises the unit under test and asserts expected behavior.

        >>> from PyICe.test_lab_core import TestChannelGroup
        >>> hasattr(TestChannelGroup, 'test_resolve_channel_list')
        True

        """
        assert False

    def test_clone(self):
        """Perform test clone operation.

        Exercises the unit under test and asserts expected behavior.

        >>> from PyICe.test_lab_core import TestChannelGroup
        >>> hasattr(TestChannelGroup, 'test_clone')
        True

        """
        assert False

    def test_write_html(self):
        """Perform test write html operation.

        Exercises the unit under test and asserts expected behavior.

        >>> from PyICe.test_lab_core import TestChannelGroup
        >>> hasattr(TestChannelGroup, 'test_write_html')
        True

        """
        assert False
